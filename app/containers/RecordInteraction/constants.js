import { createActionTypes } from 'utils/actions';

export const fetchInteractionActionTypes = createActionTypes('fetch record interaction');
export const recordInteractionActionTypes = createActionTypes('create record interaction');
